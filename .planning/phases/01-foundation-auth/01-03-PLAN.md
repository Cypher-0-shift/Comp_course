---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/features/auth/LoginForm.tsx
  - src/app/routes/ProtectedRoute.tsx
  - src/app/routes/router.tsx
  - src/app/routes/AccessDenied.tsx
  - src/app/layouts/RootLayout.tsx
  - src/components/AuthErrorBoundary.tsx
  - src/main.tsx
  - src/App.tsx
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "User can sign in with email/password on /login page"
    - "Email confirmation required — unverified users see 'Check your email' message"
    - "Invalid credentials show toast error, rate limited shows retry countdown"
    - "Successful login redirects to role-appropriate dashboard (/student, /faculty, /admin)"
    - "User can log out from any page via signOut() in useAuth"
    - "Accessing unauthorized route redirects to /access-denied with role info"
    - "Corrupted session caught by error boundary, redirects to login"
  artifacts:
    - path: "src/features/auth/LoginForm.tsx"
      provides: "Email/password form with validation, submit handler using useAuth.signIn, error toasts (invalid creds, email not confirmed, rate limited, network), loading state, 'Forgot password' link (placeholder)"
      min_lines: 100
    - path: "src/app/routes/ProtectedRoute.tsx"
      provides: "Wrapper component accepting allowedRoles[], uses useAuth, redirects unauthorized to /login?redirect=... or /access-denied based on auth state vs role mismatch"
      min_lines: 60
    - path: "src/app/routes/router.tsx"
      provides: "React Router v6 config: /login (public), /student/* (role: student), /faculty/* (role: faculty), /admin/* (role: hod,dean), /access-denied, * → /login. Uses ProtectedRoute for protected routes."
      min_lines: 80
    - path: "src/app/routes/AccessDenied.tsx"
      provides: "403 page showing current user's role, attempted path, 'Return to Dashboard' button calling redirectToDashboard()"
      min_lines: 40
    - path: "src/components/AuthErrorBoundary.tsx"
      provides: "React error boundary catching auth errors, redirects to /login with error param, shows fallback UI"
      min_lines: 50
    - path: "src/app/layouts/RootLayout.tsx"
      provides: "Root layout with AuthProvider, QueryClientProvider, Toaster (sonner), Router, ErrorBoundary"
      min_lines: 50
    - path: "src/main.tsx"
      provides: "Entry point rendering RootLayout"
      min_lines: 20
  key_links:
    - from: "src/features/auth/LoginForm.tsx"
      to: "src/shared/hooks/useAuth.ts"
      via: "uses signIn, isLoading"
      pattern: "useAuth\(\).signIn"
    - from: "src/app/routes/ProtectedRoute.tsx"
      to: "src/shared/hooks/useAuth.ts"
      via: "consumes role, isLoading, redirectToDashboard"
      pattern: "useAuth\(\).role\|isLoading\|redirectToDashboard"
    - from: "src/app/routes/router.tsx"
      to: "src/app/routes/ProtectedRoute.tsx"
      via: "wraps protected routes"
      pattern: "<ProtectedRoute"
    - from: "src/app/layouts/RootLayout.tsx"
      to: "src/app/providers/AuthProvider.tsx"
      via: "wraps app with providers"
      pattern: "<AuthProvider>"
    - from: "src/app/routes/AccessDenied.tsx"
      to: "src/shared/hooks/useAuth.ts"
      via: "uses redirectToDashboard"
      pattern: "useAuth\(\).redirectToDashboard"
---

<objective>
Build the login UI, role-based routing with ProtectedRoute, access denied page, auth error boundary, and wire all providers in the root layout.

Purpose: Complete the authentication flow (AUTH-01 signup/login, AUTH-02 persistence, AUTH-03 logout, AUTH-04 role-based access) with proper error handling, email confirmation flow, and route protection per CONTEXT.md decisions.

Output: Working end-to-end authentication — login page, role-based redirects, protected routes, logout, session error handling.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ASUS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phase-1/CONTEXT.md
@.planning/phases/01-foundation-auth/01-01-PLAN.md
@.planning/phases/01-foundation-auth/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoginForm with email/password, validation, error handling, and email confirmation flow</name>
  <files>
    src/features/auth/LoginForm.tsx
  </files>
  <action>
    Build LoginForm component using shadcn/ui components (Card, Input, Button, Label, Alert):
    1. **Form state**: email, password, rememberMe (unused — Supabase 30-day refresh covers this per CONTEXT.md)
    2. **Validation**: Zod schema — email required + valid format, password required + min 8 chars
    3. **Submit handler**:
       - Call useAuth().signIn(email, password) — but wait, signIn is in useSupabase. Actually, add signIn to useAuth that calls supabase.auth.signInWithPassword
       - Handle Supabase auth errors specifically:
         - "Invalid login credentials" → toast.error("Invalid email or password")
         - "Email not confirmed" → toast.error("Please check your email and confirm your account")
         - "Too many requests" → toast.error("Rate limited. Please wait before trying again.") + show countdown
         - Network error → toast.error("Connection issue. Please try again.")
       - On success: useAuth().redirectToDashboard(role) navigates to correct dashboard
    4. **Loading state**: Disable button, show spinner during signIn
    5. **Email confirmation flow**:
       - Detect if error is "Email not confirmed" — show inline message with "Resend confirmation email" button
       - Resend calls supabase.auth.resend({ type: 'signup', email })
    6. **Forgot password**: Link to /forgot-password (placeholder page — just show "Contact admin" for v1)
    7. **UI**: Centered card, logo/title, form fields, submit button, footer links
    8. **Accessibility**: Proper labels, autocomplete attributes, focus management, error announcements
    9. **Redirect handling**: Read ?redirect= from search params, pass to redirectToDashboard after login
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "LoginForm" || echo "Typecheck passed"</automated>
    <manual>Verify LoginForm has: Zod validation, signIn with error handling for all 4 error types, loading state, resend confirmation, redirect param handling</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    LoginForm.tsx renders email/password form with Zod validation. Handles Supabase auth errors: invalid credentials, email not confirmed (with resend), rate limited (with countdown), network errors. On success, redirects via useAuth.redirectToDashboard(). Integrates with sonner toasts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ProtectedRoute, router config, AccessDenied page, AuthErrorBoundary, and RootLayout</name>
  <files>
    src/app/routes/ProtectedRoute.tsx
    src/app/routes/router.tsx
    src/app/routes/AccessDenied.tsx
    src/components/AuthErrorBoundary.tsx
    src/app/layouts/RootLayout.tsx
    src/main.tsx
    src/App.tsx
  </files>
  <action>
    1. **src/app/routes/ProtectedRoute.tsx**:
       - Props: { children, allowedRoles: UserRole[] }
       - Use useAuth() → { role, isLoading, redirectToDashboard }
       - If isLoading: show spinner (shadcn/ui Skeleton or spinner)
       - If no session: navigate to /login?redirect=<current-path>, return null
       - If session but role not in allowedRoles: navigate to /access-denied, return null
       - If authorized: return children
       - Deep link handling: If Student accesses /faculty → redirect to /student + toast "Redirected to your dashboard"
    2. **src/app/routes/router.tsx**:
       - Create BrowserRouter with Routes:
         - /login → LoginPage (no wrapper)
         - /access-denied → AccessDenied (no wrapper)
         - /student/* → <ProtectedRoute allowedRoles={['student']}> → StudentLayout (placeholder)
         - /faculty/* → <ProtectedRoute allowedRoles={['faculty']}> → FacultyLayout (placeholder)
         - /admin/* → <ProtectedRoute allowedRoles={['hod', 'dean']}> → AdminLayout (placeholder)
         - * → navigate to /login
       - Export router config for use in RootLayout
    3. **src/app/routes/AccessDenied.tsx**:
       - Use useAuth() → { role, redirectToDashboard }
       - Display: "Access Denied", current role, attempted path (from location.state or referrer), button "Return to Dashboard" calling redirectToDashboard(role)
       - Style with shadcn/ui Card, Alert (destructive)
    4. **src/components/AuthErrorBoundary.tsx**:
       - Class component (for error boundary) or use react-error-boundary package
       - Fallback: renders error message, "Go to Login" button navigating to /login?error=session_corrupted
       - onReset: clear any corrupted localStorage session data
    5. **src/app/layouts/RootLayout.tsx**:
       - Providers in order (outer to inner):
         - QueryClientProvider (new QueryClient with defaultOptions: retry=3, staleTime=5min)
         - AuthProvider
         - Toaster (sonner: position=top-right, richColors=true)
         - AuthErrorBoundary
         - BrowserRouter with router config
       - Global styles import (already in main.tsx)
    6. **src/main.tsx**: Render RootLayout (replaces previous App.tsx content)
    7. **src/App.tsx**: Keep minimal or remove — RootLayout is the new root
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "ProtectedRoute|router|AccessDenied|AuthErrorBoundary|RootLayout" || echo "Typecheck passed"</automated>
    <manual>Verify: ProtectedRoute checks role against allowedRoles, handles loading/unauthorized correctly. Router has all 5 route groups. AccessDenied shows role and redirect button. RootLayout nests providers in correct order. main.tsx renders RootLayout.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    ProtectedRoute enforces role-based access with loading/unauthorized states. Router config maps /student, /faculty, /admin to ProtectedRoute with correct allowedRoles. AccessDenied page shows user role and redirect button. AuthErrorBoundary catches errors. RootLayout wires QueryClientProvider, AuthProvider, Toaster, AuthErrorBoundary, Router in correct order.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end authentication flow</name>
  <files>
    (verification only — no file modifications)
  </files>
  <action>
    Manual verification of complete auth flow. Run `npm run dev` and test:
    1. **Visit /login** — page loads, form renders
    2. **Invalid credentials** — submit wrong password → toast "Invalid email or password"
    3. **Valid HOD login** — use seeded hod@college.edu / password123 → redirects to /admin
    4. **Valid Dean login** — use seeded dean@college.edu / password123 → redirects to /admin
    5. **Role enforcement** — as HOD, try access /student → redirects to /access-denied showing role=hod
    6. **Deep link redirect** — as Dean, access /faculty → toast "Redirected to your dashboard" + lands on /admin
    7. **Logout** — click logout (need logout button in layout — add temporary one in RootLayout for testing) → redirects to /login
    8. **Session persistence** — refresh page on /admin → stays logged in
    9. **Email not confirmed** — create new user via Supabase dashboard (unconfirmed), try login → "Check your email" + resend button works
    10. **Idle timeout** — wait 30 min (or reduce timeout temporarily for testing) → auto logout + toast
  </action>
  <verify>
    <manual>All 10 verification steps pass. Document any issues found.</manual>
  </verify>
  <done>
    Human confirms: login works for seeded admins, role-based routing enforces correctly, logout works, session persists, error handling shows appropriate toasts, access denied page displays properly.
  </done>
</task>

</tasks>

<verification>
Overall phase verification for Plan 03:
1. `npm run dev` — app loads, /login accessible
2. Login with seeded HOD/Dean credentials → redirects to /admin
3. Role-based route protection works (unauthorized → /access-denied)
4. Logout from any page → returns to /login
5. Session persists across refresh
6. Error boundary catches auth errors
7. All 4 requirements (AUTH-01, AUTH-02, AUTH-03, AUTH-04) verified
</verification>

<success_criteria>
- Complete authentication flow works end-to-end
- Login page handles all error states per CONTEXT.md
- Role-based routing enforces Student/Faculty/HOD/Dean dashboards
- Logout accessible from any page (via temporary button in RootLayout)
- Session persists across browser sessions
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>