---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/shared/hooks/useSupabase.ts
  - src/app/providers/AuthProvider.tsx
  - src/shared/hooks/useAuth.ts
  - src/shared/utils/supabase-types.ts
autonomous: true
requirements: [AUTH-02, AUTH-04, AUTH-05]
must_haves:
  truths:
    - "Typed Supabase client provides type-safe database access matching schema.sql"
    - "AuthProvider wraps app and manages session state, idle timeout (30 min), concurrent session limit (3)"
    - "useAuth hook exposes: session, user, role, departmentId, isLoading, signOut(), redirectToDashboard(role)"
    - "Session persists across browser restarts (30-day refresh token)"
    - "Auto-refresh token enabled, tab sync via storage events"
  artifacts:
    - path: "src/shared/hooks/useSupabase.ts"
      provides: "Typed Supabase client singleton with Database type from generated types, auth helpers (signIn, signUp, signOut, getSession)"
      min_lines: 60
    - path: "src/shared/utils/supabase-types.ts"
      provides: "Generated TypeScript types from Supabase schema (Database, Tables, Enums) — placeholder for now, to be replaced by CLI generation"
      min_lines: 100
    - path: "src/app/providers/AuthProvider.tsx"
      provides: "React context provider: initializes Supabase auth, listens to auth changes, manages idle timeout timer, enforces max 3 concurrent sessions via storage event sync"
      min_lines: 120
    - path: "src/shared/hooks/useAuth.ts"
      provides: "Hook consuming AuthContext: returns { session, user, role, departmentId, isLoading, signOut, redirectToDashboard } with role extracted from user.app_metadata.role"
      min_lines: 80
  key_links:
    - from: "src/shared/hooks/useSupabase.ts"
      to: "src/shared/utils/supabase-types.ts"
      via: "imports Database type"
      pattern: "import.*Database.*from.*supabase-types"
    - from: "src/app/providers/AuthProvider.tsx"
      to: "src/shared/hooks/useSupabase.ts"
      via: "uses supabase client for auth.onAuthStateChange"
      pattern: "useSupabase\(\)"
    - from: "src/shared/hooks/useAuth.ts"
      to: "src/app/providers/AuthProvider.tsx"
      via: "consumes AuthContext"
      pattern: "useAuthContext\(\)"
---

<objective>
Create the typed Supabase client, AuthProvider for session management, and useAuth hook with role-based helpers, idle timeout, and concurrent session limits.

Purpose: Build the authentication infrastructure (AUTH-02, AUTH-04, AUTH-05) that the login UI and route guards will consume. Implements all session decisions from CONTEXT.md: 30-min idle timeout, max 3 sessions, 30-day refresh, auto-refresh, tab sync.

Output: Reusable auth primitives (client, provider, hook) with full TypeScript types.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ASUS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phase-1/CONTEXT.md
@.planning/phases/01-foundation-auth/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed Supabase client hook and database type definitions</name>
  <files>
    src/shared/utils/supabase-types.ts
    src/shared/hooks/useSupabase.ts
  </files>
  <action>
    1. **src/shared/utils/supabase-types.ts**: Create placeholder Database type interface matching schema.sql. This will later be replaced by `supabase gen types typescript --project-id <id>`. For now, manually define:
       - Database interface with public schema containing Tables for: academic_years, departments, faculty, subjects, faculty_subjects, students, enrollments
       - Each Table with Row (select), Insert, Update types
       - Enums if any (status for enrollments)
       - Use camelCase for column names matching Postgres snake_case via Supabase automatic conversion
    2. **src/shared/hooks/useSupabase.ts**: Create singleton Supabase client with TypeScript generics:
       - Import createBrowserClient from @supabase/supabase-js
       - Use Database type from supabase-types.ts
       - Read VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY from import.meta.env
       - Export `supabase` singleton instance
       - Export helper functions:
         - `signIn(email, password)` → supabase.auth.signInWithPassword
         - `signUp(email, password, metadata)` → supabase.auth.signUp
         - `signOut()` → supabase.auth.signOut
         - `getSession()` → supabase.auth.getSession
         - `getUser()` → supabase.auth.getUser
         - `onAuthStateChange(callback)` → supabase.auth.onAuthStateChange
       - Configure client: persistSession=true, autoRefreshToken=true, detectSessionInUrl=true (PKCE flow)
    3. **Environment**: Create .env.example with VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY placeholders
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "useSupabase|supabase-types" || echo "Typecheck passed"</automated>
    <manual>Verify useSupabase.ts exports typed client with Database generic, helpers have correct signatures</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    supabase-types.ts defines Database schema types. useSupabase.ts exports typed Supabase client singleton with auth helpers. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AuthProvider with session management, idle timeout, and concurrent session limit</name>
  <files>
    src/app/providers/AuthProvider.tsx
    src/shared/hooks/useAuth.ts
  </files>
  <action>
    1. **src/app/providers/AuthProvider.tsx**:
       - Create AuthContext with: session, user, role, departmentId, isLoading, signOut, redirectToDashboard
       - Initialize state: session=null, user=null, isLoading=true
       - Use useSupabase() to get client
       - **Effect 1 — Initial session**: Call supabase.auth.getSession(), set session/user, extract role/departmentId from user.app_metadata
       - **Effect 2 — Auth state listener**: supabase.auth.onAuthStateChange((event, session) => { update state; if SIGNED_OUT clear all; if session extract role/dept })
       - **Idle timeout (30 min)**:
         - Track lastActivity with useRef (update on mousemove, keydown, click, touchstart)
         - Use setInterval every 60s to check: if (Date.now() - lastActivity > 30*60*1000 && session) → call signOut(), toast "Session expired due to inactivity"
         - Cleanup listeners on unmount
       - **Concurrent session limit (max 3)**:
         - On sign in, store session info in localStorage with timestamp
         - On storage event (other tabs), sync session count
         - If > 3 active sessions for same user, sign out oldest (show toast "Max sessions reached, oldest session ended")
       - **Tab sync**: Listen to storage events for session changes, update local state
       - **Provider wrapper**: Wrap children, provide value object
       - **Error boundary integration**: Catch auth errors, redirect to login with ?error=...
    2. **src/shared/hooks/useAuth.ts**:
       - Export `useAuth()` = useContext(AuthContext) with null check
       - Return: { session, user, role, departmentId, isLoading, signOut, redirectToDashboard }
       - **signOut()**: Call supabase.auth.signOut(), clear localStorage session tracking, navigate to /login
       - **redirectToDashboard(role)**: Switch on role → '/student' | '/faculty' | '/admin', use navigate()
       - Role extraction: user?.app_metadata?.role, departmentId: user?.app_metadata?.department_id
    3. **Sonner integration**: Import toast from sonner for idle timeout and session limit notifications
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "AuthProvider|useAuth" || echo "Typecheck passed"</automated>
    <manual>Verify AuthProvider has: getSession on mount, onAuthStateChange listener, 30-min idle timer, max-3-session logic, tab sync. useAuth exports all required fields including redirectToDashboard.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    AuthProvider.tsx manages full session lifecycle: initial load, auth changes, 30-min idle timeout with toast, max 3 concurrent sessions with cross-tab sync. useAuth.ts exposes session, user, role, departmentId, isLoading, signOut(), redirectToDashboard().
  </done>
</task>

</tasks>

<verification>
Overall phase verification for Plan 02:
1. `npm run typecheck` — TypeScript compiles clean
2. AuthProvider wraps app in main.tsx (to be done in Plan 03)
3. useAuth hook provides all required fields per CONTEXT.md
4. Session persistence, idle timeout, concurrent limits implemented
5. Requirements AUTH-02 (session persistence), AUTH-04 (role access), AUTH-05 (RLS enforcement via role) addressed
</verification>

<success_criteria>
- Typed Supabase client with Database generics works
- AuthProvider initializes session, handles auth changes, enforces 30-min idle timeout and max 3 sessions
- useAuth hook exposes role, departmentId, signOut, redirectToDashboard
- All TypeScript types resolve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>