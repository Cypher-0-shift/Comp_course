// Shared utility components & API hooks for faculty/admin portals
// Presentation layer (FacultyDashboard, FacultyHeader, etc.) removed — was unrouted dead code
export { DataTable } from './components/DataTable'
export { FilterBar } from './components/FilterBar'
export { SearchInput } from './components/SearchInput'
